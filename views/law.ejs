<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yucky Politicians | Law</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/law.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
</head>

<body>
    <%- include('partials/flash')%>

        <div>
            <h1>
                <%= law.lawData.title %>
            </h1>
            <h2>
                <%= law.lawData.date %>
            </h2>
            <p>
                <%= law.lawData.summary %>
            </p>
            <p>
                <%- JSON.stringify(law.requestData) %>
            </p>

            <h3>Your representatives' vote:</h3>
            <div>
                <% if (reps.house_rep!=null) { %>
                    <h2>
                        <%= reps.house_rep %>: <%= voteData.houseRepVote %>
                    </h2>
                    <% }%>
            </div>
            <div>
                <% if (reps.senate_rep_1!=null) { %>
                    <h2>
                        <%= reps.senate_rep_1 %>: <%= voteData.senateRep1Vote %>
                    </h2>
                    <% }%>
            </div>
            <div>
                <% if (reps.senate_rep_2!=null) { %>
                    <h2>
                        <%= reps.senate_rep_2 %>: <%= voteData.senateRep2Vote %>
                    </h2>
                    <% }%>
            </div>


            <div>
                <h3>Raw vote data (ugly)</h3>
                <% if (votes==null) { %>
                    <p>Whoops! Looks like Congress did a stupid and forgot to record their voting data!</p>
                    <% } else { %>
                        <p><%- JSON.stringify(voteData.votes) %></p>
                        <% } %>
            </div>


        </div>

</body>

</html>